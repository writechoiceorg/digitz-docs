name: Sync OpenAPI definition to ReadMe

# Run workflow for every push to the `main` branch
on:
  push:
    branches:
      - main

jobs:
  sync_authorization:
    runs-on: ubuntu-latest
    if: "github.event_name == 'push' && 'openAPI/authorization.yml' in github.event.commits[*].added"
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v3

      - name: GitHub Action
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openAPI/authorization.yml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.AUTHORIZATION_ID }}

  sync_merchant:
    runs-on: ubuntu-latest
    if: "github.event_name == 'push' && 'openAPI/merchant.yml' in github.event.commits[*].added"
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v3

      - name: GitHub Action
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openAPI/merchant.yml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.MERCHANT_ID }}

  sync_payments:
    runs-on: ubuntu-latest
    if: "github.event_name == 'push' && 'openAPI/payments.yml' in github.event.commits[*].added"
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v3

      - name: GitHub Action
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openAPI/payments.yml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.PAYMENTS_ID }}

  sync_payment_split:
    runs-on: ubuntu-latest
    if: "github.event_name == 'push' && 'openAPI/payment-split.yml' in github.event.commits[*].added"
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v3

      - name: GitHub Action
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openAPI/payment-split.yml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.PAYMENT_SPLIT_ID }}

  sync_refund:
    runs-on: ubuntu-latest
    if: "github.event_name == 'push' && 'openAPI/refund.yml' in github.event.commits[*].added"
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v3

      - name: GitHub Action
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openAPI/refund.yml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.REFUND_ID }}

  sync_refund_split:
    runs-on: ubuntu-latest
    if: "github.event_name == 'push' && 'openAPI/refund-split.yml' in github.event.commits[*].added"
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v3

      - name: GitHub Action
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./openAPI/refund-split.yml --key=${{ secrets.README_API_KEY }} --id=${{ secrets.REFUND_SPLIT_ID }}